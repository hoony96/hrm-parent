spring:
  application:
    name: ES-SERVICE
  cloud:
    config:
      name: application-es  # 配置文件的名
      discovery:
        enabled: true
        service-id: CONFIG-SERVER # 配置中心注册的名
  profiles:
    active: dev

